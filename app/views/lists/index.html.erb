<div class= "container mt-3">
  <h1>Watchlists</h1>
  <% @lists.each do |list| %>
    <div>
      <li>
          <%= link_to list.name, list_path(list) %>
          <p><%= link_to 'Delete', list_path(list), data: { turbo_method: :delete, turbo_confirm: "Are you sure you want to delete #{list.name}?" } %></p>

      </li>
    </div>
  <% end %>
  <%= render 'lists/form', list: @list  %>
</div>
